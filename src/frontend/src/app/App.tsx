/**
 * Re-export the responsive AppLayout from shared/layout.
 * The router imports { AppLayout } from "./App", so the import path stays stable.
 */
export { AppLayout } from "@/shared/layout/AppLayout";
